# Add these lines to your .gitignore file to protect sensitive information

# Environment files with secrets
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Google OAuth credentials (if accidentally saved as separate files)
google-credentials.json
oauth-credentials.json
